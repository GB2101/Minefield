import { Generate } from './Generate';

import { Schemas } from './Schemas/Generate';

const GameCtrl = {
	Generate,
};

const GameSchemas = {
	Generate: Schemas,
};

export { GameCtrl, GameSchemas };
